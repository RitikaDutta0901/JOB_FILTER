This folder is intentionally left as a placeholder for data model abstractions.

You can add model classes or query helper modules here to centralize database queries
instead of keeping raw SQL in controllers.
